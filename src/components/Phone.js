import { Text } from './base'
import renderWithLabel from './RenderWithLabel';
//import withValidationIcon from './WithValidationIcon'

export default renderWithLabel(Text);
//export default withValidationIcon(renderWithLabel(Text));

